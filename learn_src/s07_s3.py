# -*- coding: utf-8 -*-
SUBJECT = {
"sec": 7,
"nm": "Amazon S3",
"tag": "Storage",
"modules": [
{
 "id": "buckets-objects",
 "nm": "Buckets, objects and consistency",
 "goal": "Describe how S3 names and stores things, and what it guarantees about them.",
 "min": 6,
 "blocks": [
  {"t":"p","x":"S3 stores **objects** in **buckets**. An object is data plus metadata, addressed by a **key** — the full path after the bucket name. There are no real folders; a slash is just a character in the key."},
  {"t":"table","head":["Thing","Rule"],
   "rows":[
    ["Bucket name","Globally unique across all of AWS, and it lives in one region"],
    ["Object key","Up to 1,024 bytes; the prefix before the last slash is what listings treat as a folder"],
    ["Object size","Up to 5 TB, and anything over 5 GB must use multipart upload"],
    ["Durability","Eleven nines, across at least three Availability Zones"],
    ["Consistency","**Strong read-after-write** for new objects, overwrites and deletes"]]},
  {"t":"key","x":"S3 is strongly consistent: a read immediately after a write returns the new object. Any answer that talks about waiting for eventual consistency in S3 is out of date."},
  {"t":"h","x":"What S3 is not"},
  {"t":"list","x":[
   "Not a file system. It has no partial writes and no rename — a rename is a copy plus a delete.",
   "Not block storage — you cannot mount it as a disk and boot from it.",
   "Not free to list: listing millions of keys costs requests and time, which is why prefixes matter."]},
  {"t":"flow","x":["Client PUT","Object stored across three or more Availability Zones","GET returns it immediately"],
   "cap":"No sync step, no propagation delay."},
  {"t":"trap","x":"Bucket names are globally unique, so a question where two accounts create the same bucket name in different regions is describing something impossible."}
 ],
 "checks":[
  {"q":"What consistency model does S3 provide for a read immediately after a write?",
   "o":["Strong read-after-write consistency","Eventual consistency for new objects",
        "Eventual consistency for overwrites only","Consistency only within one Availability Zone"],
   "a":[0],
   "x":"S3 returns the latest data for new objects, overwrites and deletes with no delay. Older material describing eventual consistency for overwrites no longer applies."},
  {"q":"How unique must a bucket name be?",
   "o":["Globally unique across all AWS accounts and regions","Unique within your account",
        "Unique within the region","Unique within the Availability Zone"],
   "a":[0],
   "x":"Bucket names live in a single global namespace because they appear in a hostname. The bucket's data, however, is stored in one region."},
  {"q":"An object is 12 GB. What does uploading it require?",
   "o":["A multipart upload","A single PUT request","Transfer Acceleration",
        "Splitting it across two buckets"],
   "a":[0],
   "x":"Anything above 5 GB must be uploaded in parts, and multipart is recommended well below that for reliability and parallelism. A single PUT cannot carry it."},
  {"q":"What does a slash in an object key represent?",
   "o":["An ordinary character that listings treat as a folder boundary",
        "A real directory created in the bucket","The boundary between bucket and region",
        "A storage class marker"],
   "a":[0],
   "x":"S3 has a flat key space. Consoles and listing APIs use the prefix before a delimiter to present something that looks like folders, but nothing hierarchical exists underneath."}
 ]
},
{
 "id":"s3-security",
 "nm":"Who can reach an object",
 "goal":"Choose between bucket policy, presigned URL and endpoint, and explain Block Public Access.",
 "min":8,
 "blocks":[
  {"t":"table","head":["Mechanism","What it is for"],
   "rows":[
    ["**Bucket policy**","A resource policy on the bucket — the main tool, and the only way to grant another account access without a role"],
    ["**IAM policy**","What an identity in your account may do"],
    ["**Block Public Access**","An account and bucket level switch that overrides everything else"],
    ["**Presigned URL**","A time-limited URL carrying the signer's permissions, for one object"],
    ["**Access Point**","A named endpoint with its own policy, for one application's slice of a big bucket"],
    ["**VPC Gateway Endpoint**","Private access to S3 from a VPC with no internet path"],
    ["ACLs","Legacy, usually disabled — prefer policies"]]},
  {"t":"key","x":"**Block Public Access wins over any bucket policy.** If it is on and a question says the bucket must serve public content, the switch has to be turned off first — that is usually the point being tested."},
  {"t":"h","x":"Presigned URLs"},
  {"t":"list","x":[
   "Generated by someone who already has permission; the URL carries that permission for a set time.",
   "The standard answer for **letting a specific user download a private object** without making the bucket public.",
   "Also works for uploads, which is how a browser can put a file straight into S3 without the file passing through your servers.",
   "Expiry is short by design; the permission ends when the URL does."]},
  {"t":"h","x":"Reaching S3 privately"},
  {"t":"flow","x":["Instance in a private subnet","VPC gateway endpoint for S3","S3 — traffic never leaves the AWS network"],
   "cap":"No NAT gateway, no internet gateway, and no data transfer charge for the NAT that is no longer there."},
  {"t":"trap","x":"\"Objects must not traverse the public internet\" is a **VPC endpoint**, not a NAT gateway. A NAT gateway does send the traffic over the internet path, and it costs more."},
  {"t":"h","x":"Cross-origin requests"},
  {"t":"p","x":"**CORS** on the bucket is what allows a web page hosted at one origin to fetch objects from another. A browser error mentioning cross-origin against an S3 URL is a CORS configuration question."}
 ],
 "checks":[
  {"q":"A private object must be downloadable by one customer for the next hour, without making the bucket public. What do you use?",
   "o":["A presigned URL","A public bucket policy","An ACL granting everyone read","Transfer Acceleration"],
   "a":[0],
   "x":"A presigned URL carries the signer's permission for a limited time and covers a single object. Nothing about the bucket's public setting has to change."},
  {"q":"A bucket policy grants public read but objects are still inaccessible. What is the most likely cause?",
   "o":["Block Public Access is enabled","The objects are in a different storage class",
        "Versioning is disabled","The bucket has no CORS configuration"],
   "a":[0],
   "x":"Block Public Access sits above bucket policies and ACLs and overrides them. Until it is turned off at the account and bucket level, no policy can make objects public."},
  {"q":"Instances in a private subnet must read from S3 without any internet path. What do you configure?",
   "o":["A VPC gateway endpoint for S3","A NAT gateway","An internet gateway with a restrictive route",
        "A presigned URL for each object"],
   "a":[0],
   "x":"A gateway endpoint adds a route that keeps S3 traffic on the AWS network. A NAT gateway would work but sends the traffic over the internet path and costs considerably more."},
  {"q":"A web page at one domain cannot fetch objects from an S3 bucket, and the browser reports a cross-origin error. What is missing?",
   "o":["A CORS configuration on the bucket","A bucket policy allowing the user",
        "A presigned URL","Versioning on the bucket"],
   "a":[0],
   "x":"The browser is enforcing the same-origin policy, so the bucket must advertise which origins may read from it. That is exactly what a CORS configuration declares."}
 ]
},
{
 "id":"versioning-replication",
 "nm":"Versioning, locking and replication",
 "goal":"Protect objects from deletion and copy them to another bucket or region.",
 "min":7,
 "blocks":[
  {"t":"h","x":"Versioning"},
  {"t":"list","x":[
   "Enabled per bucket, and once enabled it can only be **suspended**, never removed.",
   "A delete without a version id writes a **delete marker**; the object is hidden, not gone. Deleting the marker restores it.",
   "Objects present before versioning was enabled have a version id of `null`.",
   "**MFA Delete** requires a multi-factor code to delete a version or suspend versioning — and it can only be enabled by the **bucket owner using the root account**."]},
  {"t":"key","x":"Versioning is the answer to \"protect against accidental deletion and overwrite\". It is also a prerequisite for replication."},
  {"t":"h","x":"Object Lock"},
  {"t":"table","head":["Mode","Effect"],
   "rows":[
    ["Governance","Users with a special permission can still remove the lock"],
    ["Compliance","**Nobody**, including the root user, can delete before the retention expires"],
    ["Legal hold","Blocks deletion with no time limit, applied and removed per object"]]},
  {"t":"h","x":"Replication"},
  {"t":"table","head":["","Cross-region (CRR)","Same-region (SRR)"],
   "rows":[
    ["Used for","Lower latency elsewhere, compliance, disaster recovery","Log aggregation, production to test copies"],
    ["Requires","Versioning on both buckets, and an IAM role","Same"],
    ["Copies","Only objects written **after** replication is enabled","Same"],
    ["Existing objects","Need **S3 Batch Replication**","Same"],
    ["Delete markers","Replicated only if you enable it; version deletes are never replicated","Same"]]},
  {"t":"trap","x":"Replication is not retroactive. Objects that were already in the bucket are not copied unless you run Batch Replication — a favourite detail in questions about a migration that \"did not copy everything\"."},
  {"t":"note","x":"There is no chaining: if bucket A replicates to B and B replicates to C, objects from A do not reach C."}
 ],
 "checks":[
  {"q":"A user deletes an object in a versioned bucket without specifying a version. What happens?",
   "o":["A delete marker is added and the object is hidden but recoverable",
        "The object and all its versions are permanently removed",
        "The oldest version is removed","The bucket becomes read-only"],
   "a":[0],
   "x":"A delete without a version id writes a delete marker on top. Removing that marker makes the previous version current again, which is what makes versioning a protection against accidents."},
  {"q":"Records must be undeletable by anyone, including administrators, for seven years. What do you configure?",
   "o":["Object Lock in compliance mode","Object Lock in governance mode","Versioning with MFA Delete",
        "A restrictive bucket policy"],
   "a":[0],
   "x":"Compliance mode cannot be shortened or bypassed by any principal, including root, for the retention period. Governance mode allows privileged users to override it."},
  {"q":"Replication was enabled on a bucket that already held a million objects, and none of them appeared in the destination. Why?",
   "o":["Replication only copies objects written after it is enabled","The destination bucket is in another region",
        "Versioning was enabled on the destination","The objects are in a different storage class"],
   "a":[0],
   "x":"Replication is forward-looking by design. Existing objects require S3 Batch Replication to be copied, which is a separate operation."},
  {"q":"What must be true before replication can be configured?",
   "o":["Versioning is enabled on both the source and destination buckets",
        "Both buckets are in the same region","Block Public Access is disabled",
        "The buckets use the same storage class"],
   "a":[0],
   "x":"Replication tracks object versions, so versioning is required at both ends along with an IAM role that lets S3 read the source and write the destination."}
 ]
},
{
 "id":"storage-classes",
 "nm":"Storage classes and lifecycle rules",
 "goal":"Pick a class from an access pattern, and write the lifecycle rule that gets there.",
 "min":9,
 "blocks":[
  {"t":"table","head":["Class","Availability zones","Retrieval","Reach for it when"],
   "rows":[
    ["**Standard**","3 or more","Immediate","Frequently accessed data"],
    ["**Intelligent-Tiering**","3 or more","Immediate","**Access pattern is unknown or changing** — tiers move automatically for a small monitoring fee"],
    ["**Standard-IA**","3 or more","Immediate, with a retrieval charge","Accessed monthly; 30-day minimum"],
    ["**One Zone-IA**","**One**","Immediate, with a retrieval charge","Recreatable data — a zone failure loses it"],
    ["**Glacier Instant Retrieval**","3 or more","**Milliseconds**","Archives that are rare but must be instant, 90-day minimum"],
    ["**Glacier Flexible Retrieval**","3 or more","Minutes to 12 hours","Archives where a wait is fine, 90-day minimum"],
    ["**Glacier Deep Archive**","3 or more","**12 to 48 hours**","Long-term retention, cheapest, 180-day minimum"]]},
  {"t":"key","x":"The two phrases that decide most class questions: \"access pattern is unpredictable\" means **Intelligent-Tiering**, and \"can be recreated if lost\" allows **One Zone-IA**."},
  {"t":"h","x":"Lifecycle rules"},
  {"t":"list","x":[
   "**Transition actions** move objects to a cheaper class after a number of days.",
   "**Expiration actions** delete objects, old versions, or incomplete multipart uploads.",
   "Rules can be scoped to a **prefix** or to **tags**.",
   "Cleaning up incomplete multipart uploads is a real and frequently missed saving."]},
  {"t":"flow","x":["Day 0 Standard","Day 30 Standard-IA","Day 90 Glacier Flexible","Day 365 Deep Archive","Day 2555 expire"],
   "cap":"One rule can carry an object through its whole life."},
  {"t":"h","x":"Getting data out of Glacier"},
  {"t":"p","x":"Flexible Retrieval offers expedited (1–5 minutes), standard (3–5 hours) and bulk (5–12 hours). Deep Archive offers standard (12 hours) and bulk (48 hours). **Instant Retrieval** needs no restore job at all."},
  {"t":"trap","x":"Minimum storage durations are charged whether or not the object is still there. Moving an object to Glacier and deleting it a week later costs 90 days of storage, which is why lifecycle rules that transition too early can raise the bill."},
  {"t":"h","x":"Finding out what you have"},
  {"t":"p","x":"**S3 Storage Lens** gives account-wide usage and activity trends; **S3 Analytics** recommends when to transition objects to Standard-IA. Neither moves anything — a lifecycle rule does."}
 ],
 "checks":[
  {"q":"Objects are accessed unpredictably — some daily, some never. Which storage class avoids guessing?",
   "o":["Intelligent-Tiering","Standard-IA","One Zone-IA","Glacier Deep Archive"],
   "a":[0],
   "x":"Intelligent-Tiering monitors access per object and moves it between tiers automatically for a small monitoring fee. The other classes require you to know the pattern in advance."},
  {"q":"Thumbnails can be regenerated from the originals at any time, and cost matters most. Which class?",
   "o":["One Zone-IA","Standard","Glacier Instant Retrieval","Standard-IA"],
   "a":[0],
   "x":"One Zone-IA stores data in a single Availability Zone, so a zone failure can lose it. That is acceptable precisely because the data can be recreated, and it is cheaper than the multi-zone classes."},
  {"q":"Compliance archives must be kept for seven years and are almost never read, but a retrieval within 48 hours is acceptable. Which class is cheapest?",
   "o":["Glacier Deep Archive","Glacier Instant Retrieval","Standard-IA","One Zone-IA"],
   "a":[0],
   "x":"Deep Archive is the lowest-cost class and its 12 to 48 hour retrieval fits the stated tolerance. Instant Retrieval costs more precisely because it needs no restore wait."},
  {"q":"Which action reduces cost from uploads that were started but never completed?",
   "o":["A lifecycle rule expiring incomplete multipart uploads","Enabling Transfer Acceleration",
        "Turning on versioning","Enabling Intelligent-Tiering"],
   "a":[0],
   "x":"Parts of an abandoned multipart upload keep being stored and billed until something removes them. A lifecycle expiration rule is the standard cleanup and is easy to overlook."}
 ]
},
{
 "id":"s3-encryption",
 "nm":"Encryption",
 "goal":"Name the four server-side options and say which one a requirement demands.",
 "min":7,
 "blocks":[
  {"t":"table","head":["Option","Who holds the key","Choose it when"],
   "rows":[
    ["**SSE-S3**","AWS, entirely","The default; encryption is required but nobody asks about keys"],
    ["**SSE-KMS**","A KMS key you control","You need an **audit trail of key use**, key rotation, or a policy on who may decrypt"],
    ["**DSSE-KMS**","A KMS key, applied twice","A regulation demands two layers of encryption"],
    ["**SSE-C**","**You**, sent with every request","You must keep the keys entirely outside AWS; S3 never stores them, and HTTPS is mandatory"],
    ["**Client-side**","You, before upload","S3 must never see plaintext at all"]]},
  {"t":"key","x":"\"We must be able to audit every use of the key\" or \"control who can decrypt\" means **SSE-KMS**, because KMS logs each call to CloudTrail and enforces a key policy."},
  {"t":"h","x":"The KMS cost trap"},
  {"t":"p","x":"Every object read or written with SSE-KMS calls KMS, which counts against KMS request quotas and costs money. **S3 Bucket Keys** cut those calls dramatically for high-volume buckets — the standard answer when SSE-KMS causes throttling or an unexpected KMS bill."},
  {"t":"h","x":"Default encryption and enforcement"},
  {"t":"list","x":[
   "Buckets encrypt with SSE-S3 by default; default encryption can be set to a KMS key instead.",
   "To *force* a particular method, use a **bucket policy that denies PutObject** unless the request carries the right `x-amz-server-side-encryption` header.",
   "Enabling default encryption does not re-encrypt objects that are already there."]},
  {"t":"trap","x":"With SSE-C the key travels in the request, so S3 refuses the call over plain HTTP. An option that uses SSE-C over HTTP is wrong on that alone."}
 ],
 "checks":[
  {"q":"A regulator requires an auditable record of every use of the encryption key protecting S3 objects. Which option?",
   "o":["SSE-KMS","SSE-S3","SSE-C","No encryption, with bucket logging"],
   "a":[0],
   "x":"KMS records every encrypt and decrypt call in CloudTrail and enforces a key policy. SSE-S3 keys are managed entirely by AWS with no such visibility."},
  {"q":"A bucket using SSE-KMS is being throttled by KMS request limits during a large batch job. What reduces the calls?",
   "o":["Enable S3 Bucket Keys","Switch to SSE-C","Turn off default encryption",
        "Move the objects to One Zone-IA"],
   "a":[0],
   "x":"A bucket key is derived once and reused for many objects, which cuts KMS requests sharply. Switching to SSE-C moves key handling to the client rather than solving the throttling properly."},
  {"q":"Every upload must be rejected unless it specifies server-side encryption. How is that enforced?",
   "o":["A bucket policy that denies PutObject without the encryption header",
        "Enabling default encryption on the bucket","Turning on versioning",
        "Adding a lifecycle rule"],
   "a":[0],
   "x":"Default encryption silently encrypts anything that arrives unencrypted, which is not the same as rejecting it. A deny policy conditioned on the encryption header is what actually enforces the rule."},
  {"q":"Why must SSE-C requests use HTTPS?",
   "o":["The encryption key is sent in the request itself","S3 requires HTTPS for all requests",
        "Because the object is stored in one Availability Zone","Because KMS is called on every request"],
   "a":[0],
   "x":"With SSE-C you provide the key on every call, so sending it over an unencrypted connection would expose it. S3 rejects such requests outright."}
 ]
},
{
 "id":"s3-performance",
 "nm":"Performance, events and large-scale operations",
 "goal":"Speed up transfers and queries, and trigger work when an object lands.",
 "min":7,
 "blocks":[
  {"t":"h","x":"Throughput"},
  {"t":"list","x":[
   "S3 scales to at least **3,500 PUT/COPY/POST/DELETE and 5,500 GET/HEAD per second per prefix**. More prefixes means more throughput.",
   "**Multipart upload** parallelises a large upload and lets a failed part be retried alone.",
   "**Transfer Acceleration** routes uploads through the nearest CloudFront edge — the answer for users far from the bucket's region.",
   "**Byte-range fetches** parallelise a download, or fetch just the header of a large file."]},
  {"t":"key","x":"Slow uploads from a distant country → **Transfer Acceleration**. Slow uploads of very large files → **multipart**. The two are often combined."},
  {"t":"h","x":"Querying without downloading"},
  {"t":"table","head":["Tool","What it does"],
   "rows":[
    ["**S3 Select**","Runs simple SQL over a single object, returning only the rows and columns you asked for"],
    ["**Athena**","Runs SQL over many objects in a bucket, as a table"],
    ["**S3 Object Lambda**","Transforms an object as it is retrieved — redacting, converting, resizing — without storing a second copy"]]},
  {"t":"h","x":"Event notifications"},
  {"t":"flow","x":["Object created","S3 event notification","SNS, SQS, Lambda or EventBridge","Your processing runs"],
   "cap":"EventBridge adds filtering, archiving and replay, and can fan out to many targets."},
  {"t":"h","x":"Operating at scale"},
  {"t":"list","x":[
   "**S3 Batch Operations** performs an action across millions of objects — copy, tag, restore from Glacier, invoke a Lambda — driven by an inventory report.",
   "**S3 Inventory** produces a scheduled listing of objects and their metadata, which is how you audit a very large bucket without listing it live.",
   "**Storage Lens** gives usage and activity trends across the account."]},
  {"t":"trap","x":"Using Transfer Acceleration to fix slow *downloads* by many users is the wrong tool — that is **CloudFront**. Acceleration is aimed at getting data into a distant bucket."}
 ],
 "checks":[
  {"q":"Users in Australia upload large files to a bucket in Ireland and the transfers are slow. What helps most?",
   "o":["S3 Transfer Acceleration","A lifecycle rule","S3 Select","Versioning"],
   "a":[0],
   "x":"Transfer Acceleration sends the upload to the nearest edge location and then over the AWS backbone, which is exactly the long-distance upload problem. Lifecycle and versioning have nothing to do with speed."},
  {"q":"An application needs a few columns from a large CSV object without downloading all of it. What should it use?",
   "o":["S3 Select","S3 Batch Operations","Transfer Acceleration","S3 Inventory"],
   "a":[0],
   "x":"S3 Select runs a simple SQL expression inside S3 and returns only the matching data, which cuts both transfer time and cost for a single object."},
  {"q":"A thumbnail must be generated every time an image is uploaded. What triggers it?",
   "o":["An S3 event notification invoking a Lambda function","A lifecycle transition rule",
        "A bucket policy","S3 Storage Lens"],
   "a":[0],
   "x":"Event notifications fire on object creation and can invoke Lambda directly, or go through SNS, SQS or EventBridge. Lifecycle rules act on age, not on arrival."},
  {"q":"Tags must be applied to fifty million existing objects. What performs the change?",
   "o":["S3 Batch Operations driven by an inventory report","A lifecycle rule",
        "Enabling replication","A bucket policy"],
   "a":[0],
   "x":"Batch Operations runs a single action across a manifest of objects at scale, with progress reporting and retries. Nothing else in S3 applies an action to an existing set of objects."}
 ]
}
],
"recap":[
 {"t":"h","x":"Storage class, decided by one sentence"},
 {"t":"dtree","x":[
  ["Access pattern is unknown or changes","Intelligent-Tiering"],
  ["Data can be recreated if lost","One Zone-IA"],
  ["Read about once a month","Standard-IA"],
  ["Archive, but retrieval must be instant","Glacier Instant Retrieval"],
  ["Archive, minutes to hours is fine","Glacier Flexible Retrieval"],
  ["Archive for years, 12 to 48 hours is fine","Glacier Deep Archive"],
  ["Frequently read","Standard"]]},
 {"t":"h","x":"Access, in order of precedence"},
 {"t":"table","head":["Control","Beats"],
  "rows":[["Block Public Access","Everything else — bucket policy and ACLs included"],
          ["Explicit Deny in a policy","Any Allow"],
          ["Bucket policy","The default of no access"],
          ["Presigned URL","Nothing — it carries the signer's own permissions"]]},
 {"t":"key","x":"Private access from a VPC is a **gateway endpoint**. One customer, one object, one hour is a **presigned URL**. Another account is a **bucket policy**. Public content requires Block Public Access to be off."},
 {"t":"h","x":"Encryption"},
 {"t":"table","head":["Requirement","Option"],
  "rows":[["Just encrypt it","SSE-S3"],
          ["Audit and control key use","SSE-KMS"],
          ["Two layers required by regulation","DSSE-KMS"],
          ["Keys must stay outside AWS","SSE-C, over HTTPS only"],
          ["S3 must never see plaintext","Client-side encryption"],
          ["KMS throttling or cost","Enable S3 Bucket Keys"],
          ["Reject unencrypted uploads","Deny PutObject without the encryption header"]]},
 {"t":"h","x":"Protection and copies"},
 {"t":"split","cols":[
  {"nm":"Against deletion","items":["Versioning, with delete markers","MFA Delete, enabled by the root user only","Object Lock: governance, compliance, legal hold"]},
  {"nm":"Copies elsewhere","items":["CRR for another region, SRR for the same one","Versioning required on both sides","Only new objects — existing ones need Batch Replication","No chaining from A to B to C"]}]},
 {"t":"h","x":"Performance and scale"},
 {"t":"table","head":["Problem","Answer"],
  "rows":[["Slow uploads from far away","Transfer Acceleration"],
          ["Very large single upload","Multipart upload"],
          ["Slow downloads for many users","CloudFront, not Transfer Acceleration"],
          ["Need a few rows from one object","S3 Select"],
          ["SQL across a whole bucket","Athena"],
          ["Transform on retrieval","S3 Object Lambda"],
          ["Act on millions of existing objects","S3 Batch Operations"],
          ["React when an object arrives","Event notification to Lambda, SQS, SNS or EventBridge"]]},
 {"t":"trap","x":"Recurring distractors: a bucket policy offered where Block Public Access is the blocker; replication assumed to copy existing objects; Transfer Acceleration offered for downloads; a NAT gateway where a VPC endpoint is required; and default encryption confused with enforcing encryption."}
],
"final":[
 {"m":"s3-security",
  "q":"A bucket must serve a public website, but objects return access denied even though the bucket policy grants public read. What must change?",
  "o":["Turn off Block Public Access at the account and bucket level",
       "Add an ACL granting read to everyone","Enable static website hosting only","Switch the objects to Standard storage class"],
  "a":[0],
  "w":["Correct — Block Public Access overrides bucket policies and ACLs, so nothing takes effect until it is disabled.",
      "ACLs are also overridden by Block Public Access, and they are the legacy mechanism in any case.",
      "Website hosting changes how the endpoint behaves but does not grant access.",
      "Storage class has no bearing on permissions."],
  "x":"Block Public Access is a deliberate override that sits above every other S3 permission mechanism. The exam uses it to test whether you know the order of precedence rather than just the policy syntax."},
 {"m":"storage-classes",
  "q":"Logs are read heavily for 30 days, occasionally for the next 60, then almost never but must be kept for seven years with retrieval within a day acceptable. Which lifecycle is correct?",
  "o":["Standard, then Standard-IA at 30 days, then Glacier Deep Archive at 90 days, expire at seven years",
       "Standard for seven years, then delete","Glacier Deep Archive immediately, restoring as needed","One Zone-IA immediately, then Standard-IA at 90 days"],
  "a":[0],
  "w":["Correct — each transition matches the access pattern described, and the retrieval tolerance allows the cheapest archive tier.",
      "Keeping everything in Standard for seven years is by far the most expensive option.",
      "Archiving data that is read heavily for its first month forces constant restores and early-deletion charges.",
      "One Zone-IA risks losing logs to a zone failure, and moving to a more expensive class later is backwards."],
  "x":"Lifecycle questions are read straight off the access pattern in the text. The stated 24-hour retrieval tolerance is what permits Deep Archive rather than Glacier Instant Retrieval."},
 {"m":"versioning-replication",
  "q":"After enabling cross-region replication, a team finds that only recently uploaded objects exist in the destination bucket. What explains this and what fixes it?",
  "o":["Replication applies only to new objects; run S3 Batch Replication for the existing ones",
       "The destination region does not support replication; choose another","Versioning is disabled on the source; enable it and wait","Delete markers blocked the copy; disable versioning"],
  "a":[0],
  "w":["Correct — replication is forward-looking, and Batch Replication is the supported way to copy what was already there.",
      "All commercial regions support replication; this is not a regional limitation.",
      "Replication cannot be configured at all without versioning on the source, so it would never have started.",
      "Disabling versioning would break replication rather than fix it."],
  "x":"This catches teams during migrations. The configuration is correct; it simply does not reach backwards, which is exactly what S3 Batch Replication exists for."},
 {"m":"s3-encryption",
  "q":"A high-volume pipeline writing millions of objects with SSE-KMS starts hitting KMS request throttling. What is the correct response?",
  "o":["Enable S3 Bucket Keys on the bucket",
       "Switch the bucket to SSE-C","Disable encryption for the pipeline's prefix","Move the bucket to another region"],
  "a":[0],
  "w":["Correct — a bucket key is derived once and reused, cutting per-object KMS calls dramatically.",
      "SSE-C shifts key management to the client and introduces a far larger operational problem.",
      "Turning off encryption to avoid throttling abandons the security requirement.",
      "Region changes do not alter per-request KMS quotas."],
  "x":"SSE-KMS calls KMS for every object, which is what runs into request quotas at scale. Bucket Keys were introduced for this case and usually cut KMS costs by an order of magnitude too."},
 {"m":"s3-security",
  "q":"EC2 instances in private subnets download objects from S3 through a NAT gateway, and the NAT data processing charges are large. What reduces them while keeping traffic off the internet?",
  "o":["Create a VPC gateway endpoint for S3 and route S3 traffic through it",
       "Give the instances public IP addresses","Use presigned URLs for each download","Enable Transfer Acceleration on the bucket"],
  "a":[0],
  "w":["Correct — a gateway endpoint keeps S3 traffic on the AWS network and removes the NAT data processing charge entirely.",
      "Public addressing exposes the instances and still routes over the internet path.",
      "Presigned URLs change authorisation, not the network path or its cost.",
      "Transfer Acceleration targets long-distance uploads and would add cost here."],
  "x":"Gateway endpoints for S3 and DynamoDB cost nothing and are the standard answer to both \"must not traverse the internet\" and \"NAT charges are too high\"."},
 {"m":"buckets-objects",
  "q":"An application writes an object and immediately reads it back from a different instance. What does S3 guarantee?",
  "o":["The read returns the object just written, because S3 is strongly consistent",
       "The read may return nothing for a short period","The read returns an older version until caches expire","Consistency only if both instances are in the same Availability Zone"],
  "a":[0],
  "w":["Correct — S3 provides strong read-after-write consistency for new objects, overwrites and deletes.",
      "That behaviour described S3 many years ago and no longer applies.",
      "There is no propagation window for overwrites now.",
      "Consistency is not scoped to an Availability Zone."],
  "x":"S3 became strongly consistent for all operations, which removed a whole class of workaround patterns. Answers that still describe eventual consistency are relying on outdated material."},
 {"m":"s3-performance",
  "q":"A reporting tool must run SQL across millions of objects in a bucket as if they were a table. Which service fits?",
  "o":["Amazon Athena","S3 Select","S3 Batch Operations","S3 Inventory"],
  "a":[0],
  "w":["Correct — Athena queries data in S3 as tables across many objects, without loading it anywhere.",
      "S3 Select operates on one object at a time and supports only simple expressions.",
      "Batch Operations performs actions on objects; it does not answer queries.",
      "Inventory lists objects and metadata rather than querying their contents."],
  "x":"The scope decides it: one object is S3 Select, a whole dataset is Athena. Both leave the data in S3 rather than copying it into a database."},
 {"m":"versioning-replication",
  "q":"Financial records must be immutable for five years, and the requirement explicitly says no user or administrator may delete them early. What satisfies it?",
  "o":["S3 Object Lock in compliance mode with a five-year retention",
       "S3 Object Lock in governance mode with a five-year retention","Versioning with MFA Delete enabled","A bucket policy denying s3:DeleteObject"],
  "a":[0],
  "w":["Correct — compliance mode cannot be shortened or overridden by anyone, including the root user.",
      "Governance mode is designed to be overridable by users holding a specific permission.",
      "MFA Delete requires a code but does not prevent a determined root user from deleting.",
      "A bucket policy can be edited by anyone who can change policies, so it is not immutability."],
  "x":"The phrase \"including administrators\" rules out every mechanism that someone with enough privilege can undo. Only compliance mode makes the retention absolute."},
 {"m":"s3-performance",
  "q":"Objects must be redacted differently for different applications, without storing a second copy of each object. What provides this?",
  "o":["S3 Object Lambda, which transforms objects as they are retrieved",
       "A lifecycle rule that rewrites objects","Cross-region replication into a redacted bucket","S3 Select with a filter expression"],
  "a":[0],
  "w":["Correct — Object Lambda runs your function on the retrieval path and returns the transformed result.",
      "Lifecycle rules move and expire objects; they never modify content.",
      "Replication would create exactly the second copy the requirement forbids.",
      "S3 Select filters rows and columns of structured data but cannot apply arbitrary redaction logic."],
  "x":"Object Lambda exists for \"one copy, many views\": the stored object stays canonical and each access point can present it differently, which also keeps the redaction logic in one place."},
 {"m":"storage-classes",
  "q":"A team moved objects to Glacier Flexible Retrieval after seven days and deleted most of them a month later. The bill went up rather than down. Why?",
  "o":["Glacier classes have a 90-day minimum storage charge, applied even after deletion",
       "Glacier is more expensive than Standard for all data","Lifecycle transitions are charged per gigabyte per day","Deleting objects in Glacier requires a restore first"],
  "a":[0],
  "w":["Correct — objects deleted before the minimum duration are still billed for the remainder of it, plus a transition request charge.",
      "Glacier storage is far cheaper per gigabyte; the problem is the minimum duration, not the rate.",
      "Transitions are charged per request, not per gigabyte per day.",
      "Deletion does not require a restore, though reading the data would."],
  "x":"Minimum storage durations — 30 days for IA, 90 for Glacier tiers, 180 for Deep Archive — make aggressive early transitions counterproductive for short-lived data."}
]
}
